
# Quanta Developer Documentation

This repository contains the source code for the Quanta Developer Documentation website. 

## Running the code

Run `npm install` at the root directory to install the dependencies.

Run `npm run dev` to start the development servers for both the frontend and backend simultaneously.